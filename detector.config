# If you don't specify an exact path for the log, make sure you run the
# program from the same file directory as the SSH log
LOG_FILE = ssh.log
FILE_TYPE = sshd
INTERVAL = 5
MAX_ATTEMPTS = 10
SERVER = db_warehouse

